# PRISM 界面优化记录

本文件持续记录每一步优化：问题、改动、原因和验证结果。后续修改请继续追加，不覆盖历史。

## 2026-09-05 · 01 / 阅读背景与确认现状

- 实际项目路径：`F:\vibe_projects\prism-image-particle-lab`。阅读了 `docx/提示词.txt`、`index.html`、`styles.css`、`app.js` 和 `package.json`。
- 项目为 Vite + 原生 HTML/CSS/JavaScript，使用 Three.js 重建图像粒子、GSAP 执行转场，现有 Lucide 图标继续复用。
- 已有功能：图片选择/拖入、三种空间模式、景深/流动/粒径调节、散开与聚合、暂停、拖动旋转、滚轮缩放、重置视角。
- 仓库已有未提交改动；以当前工作区为基础优化，保留用户已有修改。
- 旧的 `current-baseline.png` 与当前源码不一致，不能用作本轮改动前截图；将重新启动页面记录基线。
- 采用 `redesign-existing-projects`、`design-taste-frontend` 技能。按用户要求优先降低界面密度和文字强度，保持现有技术栈与粒子艺术效果。

## 2026-09-05 · 02 / 布局与排版方案

- 问题：顶部玻璃导航、大号居中标题、底部控制条与粒子争夺空间；默认粒子为避让标题而下移。
- 方案：桌面使用左侧介绍/图像、中央独立画布、右侧操作的三列布局。控件不覆盖中央画布，粒子回到画布中心。
- 排版：缩小标题，压缩说明，保留可读的暖灰文字；只用一个克制的暖色强调。撤去玻璃容器、彩色按钮和无意义的标签。
- 操作：选择图片使用简洁加号；移除重复导入入口与“仅在本地处理”提示；模式名称保持可见，参数按需展开。
- 响应式：窄屏将介绍压缩到上方，操作保持右侧窄列，图片信息位于独立边缘区域；画布仍有自己的布局空间。
- 验证计划：保存真实改动前/后截图；检查桌面、平板、手机、矮窗口与横屏，回归图片导入、所有控件、键盘焦点和生产构建。

## 2026-09-06 · 03 / 三栏沉浸式工作台

- 将顶部导航、居中 Hero 和底部控制坞重排为左侧叙事、中央作品、右侧控制的三栏结构，中央粒子画布不再被界面覆盖。
- 标题收束为「让图像，成为光。」，字号降至 23–28px；说明缩为两行。去掉顶部重复导入入口、「仅在本地处理」标签和标题内嵌图片。
- 「选择图片」改为低对比描边按钮，右侧采用垂直加号，移除斜箭头和磁吸位移。
- 设计系统收束为炭黑、暖灰、柔和铜粉单强调色；去除大面积玻璃、圆角胶囊和彩色多强调，改用细线、留白和窄侧栏建立层级。
- 粒子依据中央画布独立尺寸渲染；默认回到垂直中心，镜头按可见粒子的实际包围盒和画布宽高比自动适配，透明边距不再导致主体过小。
- 移动端改为上方品牌/导入、中央独立画布、右侧图标控制、下方图像信息；参数展开时只拓宽右侧栏。矮横屏取消固定高布局，控制栏可在自身区域滚动。
- 参数面板收起时使用 `inert` 阻止键盘误入，关闭后焦点回到触发按钮；全局空格快捷键不再抢占按钮和表单控件，画布增加方向键旋转。
- 修复暗图回退时透明像素被误计入粒子数量的问题；完全透明图片现在给出明确错误。
- 一度尝试 `mix-blend-mode: screen` 提升粒子亮度，Chromium 实测导致透明 WebGL 画布合成为白色，已立即撤销并记录，未保留到交付代码。

### 验证

- 自动化覆盖 1440×900、1280×720、390×844、320×568、844×390；检查布局边界、参数焦点、模式、滑杆、散开/聚合、暂停、复位、键盘、图片选择、拖放和错误状态。
- 首轮 141 项中 139 项通过；两项为移动端隐藏文字导致测试使用 `innerText` 取不到「聚合」，功能与 aria 状态均正常。测试改用 `textContent` 后重跑，141/141 全部通过。
- `npm run build` 通过。当前仅有 Vite 对单一 Three.js 入口大于 500kB 的提示，后续模块化与懒加载阶段处理。

## 2026-09-06 · 04 / 市场调研与 PRD v1.1

- 调研浏览器端图像粒子产品、开源编辑器、通用粒子框架与 Three.js/WebGPU 官方能力，完整结论与来源写入 `docs/MARKET_RESEARCH.md`。
- 竞品共性：图片上传、密度/速度/颜色、鼠标互动已是基础能力；成熟编辑器进一步提供预设、本地保存、视频/图片/嵌入导出、边缘流场与性能档。
- 产品取舍：PRISM 不走 20+ 参数同屏的全参数路线，定位为「作品优先的空间暗房」，核心流程改为“导入 → 自动显影 → 选择气质 → 微调 → 定格 → 导出”。
- PRD 更新到 v1.1，修正旧版顶部导航、底部 dock、Hero 淡出、固定镜头等已过时描述，并纳入当前三栏布局和自动包围盒构图。
- 新增 F10 捕捉与导出、F11 气质预设、F12 自适应质量、F13 连续重组四组需求，补充验收规则和版本完成定义。
- WebGPU 仅作为渐进增强：MDN 仍标记为 Limited availability，近期保留 WebGL 稳定基线；视频导出、GLB/视频输入和社区功能暂缓，先完成静态作品闭环。

## 2026-09-06 · 05 / F10 定格与 2× PNG 导出

- 右侧新增「定格」入口。进入后两侧叙事和控制退出布局，Canvas 使用完整视口并自动重新构图；右上只保留「导出 2× PNG」和退出。
- 导出基于当前作品、模式、旋转与缩放状态，将渲染器和后处理管线临时提升到 2×，最长边限制 4096px，生成无 UI 的 PNG 后恢复原 DPR、尺寸与镜头。
- 导出文件名包含源图和 ISO 时间；完成反馈包含文件大小与像素尺寸；重复点击在导出期间禁用，异常路径保证恢复渲染状态。
- `Escape` 优先退出定格并把焦点还给入口；定格工具栏在非定格状态为 `inert`，不进入 Tab 路径。
- 实际检查 1440×900 定格截图，作品居中、UI 不进入画面主体；工具栏只占右上小区域。
- 自动化从 141 项扩展到 146 项，覆盖定格布局、键盘退出、下载事件、文件名和尺寸反馈，146/146 通过；`npm run build` 通过。

## 2026-09-06 · 06 / F11 气质预设与本地收藏

- 新增四个内置气质：显影、潮汐、游离、余烬。每个气质同时编排形态、景深、流动和粒径，使用 0.72s 连续插值完成变化。
- 预设入口位于右侧栏最上方，视觉权重低于作品；桌面显示完整名称，移动端折叠时仍显示当前气质，不再出现无意义空白方块。
- 用户手动切换形态或参数后，选择器标记为「当前微调」，清楚区分预设与编辑状态。
- 支持保存最多 12 个自定义气质、刷新后恢复、删除并回到「显影」。数据以 `prism.presets.v1` 保存，损坏数据自动回退为空列表。
- 放弃浏览器原生 prompt，改为页面内小型命名对话框；可点击遮罩或取消关闭，主键盘快捷键在对话框打开时不抢占输入。
- 导出文件名现在包含源图、当前气质和时间，方便多个结果归档。
- 自动化增加内置预设参数联动、自定义状态、保存、刷新持久化与删除验证；151/151 通过，生产构建通过。

## 2026-09-06 · 07 / 粘贴导入与双指缩放

- 支持直接粘贴剪贴板中的图片；焦点在输入框、文本域或可编辑内容时不劫持粘贴，其他剪贴板内容保持浏览器默认行为。
- 多指交互用 Pointer Events 统一实现：单指维持原有旋转，第二指加入后锁定起始距离与镜头距离，通过指距比例缩放；抬起一指后平滑回到单指旋转。
- 双指与滚轮使用同一套相对适配距离限制（0.55–1.8 倍），不同长宽比图片不会套用固定镜头边界。
- Canvas 发布只读的当前旋转目标和镜头距离数据属性，既便于自动化验证，也为后续无障碍状态说明和嵌入控制提供稳定观察点。
- 调整浏览器测试：行为验收默认不批量截图，视觉截图按需单独开启，避免 Windows headless Chromium 软件渲染器在大量 WebGL 截图后丢失上下文。
- 自动化新增剪贴板图片和两指缩放验证，最终 150/150 通过；生产构建通过。

## 2026-09-06 · 08 / F12 自适应质量

- 参数面板新增自动、轻盈、均衡、精致四档质量。自动默认以均衡开始；用户选择写入 `prism.quality.v1`，刷新后继续使用。
- 每档联合控制采样预算、桌面/移动 DPR 上限和 Bloom 强度，而不是只改变单一像素比：轻盈优先流畅，精致将桌面采样预算提高到 42,000 并允许 2× DPR。
- 自动模式按 120 帧窗口测量实际 FPS；持续低于桌面 48fps/移动 23fps 时逐级降档，高于目标并连续稳定四个窗口才恢复，避免档位来回抖动。
- 自动变化保持用户的形态、参数和源图，只重建粒子预算与渲染成本；降级/恢复以一次克制 toast 反馈。
- 质量切换使用 idle callback 安排重建，并用 pending 锁避免同时进行多次自动调整。
- 自动化验证精致与轻盈产生不同粒子预算且选择被持久化；全套 151/151 通过，生产构建通过。

## 2026-09-06 · 09 / Particular Drift 对照与 PRD v1.2

- 阅读 HuntScreens 产品页、Particular Drift 仓库说明与演示文案。它的有效部分是边缘流场、受欢迎的随机化、图片/视频导出和快捷键；缺口是 2D 平面、参数墙、控件压在作品上。
- 市场调研补写专项对照表：PRISM 用三维漩涡/流场回答平面流动，用「灵感」回答无约束骰子，用定格+配方回答直接截图。
- PRD 升至 v1.2。自主增补 F14 灵感、F15 快捷键图例、F16 配方/URL；把漩涡/流场从 P2 提前；取消 Google Fonts；明确主按钮不做磁吸。
- 字体与文案原则改为：系统衬线标题、更低对比、说明文字可缺席，让中央粒子成为唯一光源。

## 2026-09-06 · 10 / 暗房排版再收束

- 去掉 Google Fonts，标题改用系统衬线（Palatino / 宋体），界面用系统无衬线。离线与国内网络不再改变字宽。
- 删除左侧说明段落、拖入提示和画布常驻 hint；品牌字号与对比继续下降。选择图片保持垂直加号，无斜箭头。
- 手机首行只保留 PRISM 与选择图片，标题不再与作品争夺横向空间。
- Toast 移到顶部居中，避免压住左下源图信息。
- 参数面板设最大高度并内部滚动，避免展开后把散开/定格挤出屏幕。矮横屏缩短按钮并取消垂直居中，使侧栏可滚动。
- 「当前微调」不再使用 disabled option，避免 Windows 下拉显示为空。

## 2026-09-06 · 11 / 漩涡、流场、灵感、画面与配方

- Shader 增加 `uAlt`、色温、饱和，以及沿亮度梯度的 `aFlow`。漩涡绕中心螺旋，流场沿轮廓游走，并保留少量浮雕以免图像不可辨认。
- 「灵感」从六组设计过的种子气质中取样并小幅抖动，结果标为当前微调。快捷键 `R`。
- 调整面板增加辉光、色温、饱和与恢复默认。气质快照、覆盖保存与 URL hash 同步这些值。
- 定格模式可导出 2× PNG、WebP 和不含原图的 JSON 配方。`S` 在定格中导出 PNG。`?` 打开快捷键图例。
- 换图先沿散开场解构，再揭示新图；减少动效时跳过飞散。

### 验证

- 自动化 175/175 通过，覆盖五形态、灵感、配方下载、帮助对话框、画面滑杆、定格、导入与多视口。
- 目视 1440×900、390×844、844×390：作品不被侧栏遮挡；漩涡形态可辨认为螺旋结构；定格后仅右上导出条。
- `npm run build` 在提交前执行。

## 2026-09-06 · 12 / GitHub 展示整理

- 补充 MIT 许可、Pages 部署工作流、README 截图与功能表。
- 工作台封面更新为当前三栏暗房界面。

## 2026-09-06 · 13 / 底片柜与朱印界面

- 气质从右侧窄下拉改为左侧底片柜：整行衬线底片、编号、全宽「收藏气质 / 移出」，宽度约 230–290px。
- 新增「作品」收藏：把当前画面收成 JPEG 联系表，存在 IndexedDB；点击可查看、套用配方或移出。定格工具栏和 `B` 也能收藏。
- 视觉改为灯黑底 + 朱印色块「棱」、取景框角标、右侧编号形态，去掉拥挤的气质下拉和 30px 收藏图标。
- 修了 `hidden` 被 `display:flex` 覆盖导致气质/作品叠在一起、以及 dialog 默认显示的问题。
- 自动化 179/179 通过。

## 2026-09-06 · 14 / 修复 GitHub Pages 资源路径

- 生产构建把 JS/CSS 写成了站点根路径 `/assets/...`，项目页 `...github.io/prism-image-particle-lab/` 会全部 404，页面只剩未样式化的空壳。
- 增加 `vite.config.js` 的 `base: './'`，示例图改为 `import.meta.env.BASE_URL`，重新构建后资源为相对路径。

## 2026-09-06 · 15 / 解码报错与柔和收藏柜

- 修复损坏图片导致 `createImageBitmap()` 长时间挂起的问题。统一使用带 `onerror` 与对象 URL 回收的图像解码流程；损坏文件现在会在短时间内提示「无法读取这张图片」，并保证「正在重建图像」状态退出，当前作品保留。
- 收藏柜扩大为可读的左侧创作 shelf：气质收藏与作品收藏保持独立 tab，气质项增加柔和色点和更大的点击区域，作品缩略图保留双列联系表结构；主画布仍位于独立中央区域。
- 界面改为低对比深色材质：灰黑底、柔和珊瑚/雾蓝/淡紫/鼠尾草色点缀、圆角玻璃表面与更弱的边框阴影，降低朱印红的压迫感，同时保留 PRISM 的暗房识别度。
- 视觉参考取自用户提供的低对比柔光图像，只提取柔和、留白、低饱和层次，不复用图像内容；没有新增本地专属处理提示。

### 验证

- `npm run build` 通过。
- Playwright 复现损坏 PNG：toast 为「无法读取这张图片」，processing 不再处于 visible，页面无 uncaught error。
- `node tests/layout.mjs` 已完成五个视口检查；长流程在 Windows headless WebGL 结束阶段可能超时，需在 CI 再完整跑一次。

## 2026-09-06 · 16 / 粒子主画面恢复保护

- 针对空画布反馈检查默认图像加载、粒子构建与 WebGL context 状态；默认图像在本地与生产构建均能生成 `9,890` 个粒子，浏览器控制台无异常。
- 为粒子 canvas 增加 `webglcontextlost` / `webglcontextrestored` 处理：图形环境中断时暂停并提示，恢复后重新调整尺寸、重建当前图像并恢复动画，避免只剩空黑画布。
- 默认图像加载失败时主动清除 processing 状态并展示可读错误，不再留下无限等待状态。

### 验证

- `npm run build` 通过。
- Playwright 1440×900 启动验证：默认源图显示 `9,890 个粒子`，无 page error。

## 2026-09-06 · 17 / 真实浏览器空画布修复

- 用未注入软件 WebGL 参数的 Chrome 真实复现：此前 `powerPreference: high-performance` 会触发图形上下文丢失，页面仍显示粒子数量但画布实际为空；这与截图中的空黑画布一致。
- 将渲染器改为 `low-power`，让集成显卡、远程浏览器和嵌入式环境优先使用稳定上下文；质量档位仍独立控制采样预算和 DPR，不牺牲可调节性能。

### 验证

- 默认 Chrome（无 SwiftShader 参数）启动后截图确认粒子可见。
- 默认 Chrome 与软件 WebGL 两种模式均显示 `9,890 个粒子`，无 page error；截图保存于 `docs/visual-checks/default-after-lowpower.png`。

## 2026-09-06 · 18 / 超大画布 DPR 保护

- 针对高分辨率显示器仍可能空画布的情况，增加画布面积保护：当中央画布超过约 150 万 CSS 像素时，将渲染 DPR 限制为 1，避免一次性创建过大的 WebGL framebuffer 触发上下文丢失。
- 普通视口继续使用质量档位的 DPR；仅超大画布降为稳定模式，不影响粒子采样数量和交互。

## 2026-09-06 · 19 / 嵌入浏览器粒子后备渲染

- 内置浏览器仍可能创建 WebGL context 但不输出像素，单靠 context 丢失事件无法覆盖。新增位于 3D canvas 下方的 2D 点阵 canvas：从同一源图抽样并绘制彩色粒子轮廓。
- 首屏优先显示后备点阵；检测到 WebGL 有实际像素输出后平滑隐藏，保留原有三维交互。输出为空、context 丢失或恢复失败时，后备点阵持续可见，主功能不再黑屏。
- 以 2405×1260 视口验证：默认源图可见、无 page error；构建通过。

## 2026-09-06 · 20 / 默认源图加载链路

- 用户截图表明左下角持续显示「正在采样」，根因是嵌入浏览器的默认 `<img src>` 没有完成 `onload`，粒子构建从未开始。
- 默认图改为 `fetch → Blob → object URL → Image`：先确认本地资源的 HTTP 状态，再解码；增加 8 秒超时和可读错误，避免无期限等待。
- 以 2414×1303 启动验证：`flower-signal.png` 请求为 HTTP 200，源图状态更新为 `9,890 个粒子`。

## 2026-09-06 · 21 / 作品档案重构

- 按反馈将「收藏」明确为制作完成的作品档案，而非参数预设。左栏现在默认只呈现「个人档案 / 作品收藏」与「收藏此刻」；空状态说明作品可再次打开、查看并恢复当时画面。
- 内置气质与参数收藏退到非主视觉的兼容层，不再占用操作界面；用户保存的成品以缩略图墙展示，点击直接进入作品查看弹窗。
- 右栏从五种形态收束为浮雕、波面两种可读空间语言，移除星尘、漩涡、流场三个直接入口，同时缩小灵感随机样本集。
- 左上角朱印文字标识换为三片半透明彩色玻片组成的 PRISM mark；整体改用煤青底、珊瑚、雾蓝、淡紫与琥珀的胶片色彩，加入不对称色场，保留中央作品的视觉优先级。

### 验证

- `npm run build` 通过。
- 浏览器验证：仅两个形态按钮、作品库默认可见；保存作品后生成缩略图，点击可打开作品详情；无 page error。

## 2026-09-06 · 22 / 成品收藏而非参数复刻

- 移除作品详情中的「套用配方」。点击作品卡只打开当时存下的完整成品图；详情可下载原作或移出收藏，不再把参数套回当前编辑器。
- 兼容粒子模式下，收藏和下载改从实际可见的 2D 点阵画布生成图像，避免 WebGL 输出异常时把黑图存入作品库。
- 浏览器验证：保存作品、点击缩略图后详情图已加载（817×831），不存在 `print-apply` 参数按钮，页面无异常。
